# ========================
# Server Configuration
# ========================
# General server settings. Usually, you can keep NODE_ENV=development and API_PREFIX as is.
NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1

# ========================
# Service URLs
# ========================
# URLs and timeouts for internal services. Usually defaults are fine for local development.
# Make sure the URLs match the actual ports in your setup.
AUTH_SERVICE_URL=http://localhost:3001
AUTH_SERVICE_TIMEOUT=30000

SCHOOL_SERVICE_URL=http://localhost:3002
SCHOOL_SERVICE_TIMEOUT=30000

PROFILE_SERVICE_URL=http://localhost:3003
PROFILE_SERVICE_TIMEOUT=30000

ATTENDANCE_SERVICE_URL=http://localhost:3004
ATTENDANCE_SERVICE_TIMEOUT=30000

PAYMENT_SERVICE_URL=http://localhost:3005
PAYMENT_SERVICE_TIMEOUT=30000

# ========================
# JWT Configuration
# ========================
# Secret for signing JWT tokens. Generate a secure random string using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: JWT_ACCESS_SECRET must match the one in .env for auth-service
JWT_ACCESS_SECRET=your_access_secret_here

# ========================
# CORS Configuration
# ========================
# Add frontend URLs that will call this backend service.
CORS_ORIGIN=http://localhost:8080,http://localhost:8081

# ========================
# Rate Limiting
# ========================
# Limit the number of requests per window to prevent abuse.
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ========================
# Proxy Configuration
# ========================
# Timeouts and retries for proxy requests between services.
PROXY_TIMEOUT=30000
PROXY_RETRIES=3
