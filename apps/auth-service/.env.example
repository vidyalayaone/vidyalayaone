# ========================
# Server Configuration
# ========================
# General server settings. Usually, you can keep NODE_ENV=development and API_PREFIX as is.
NODE_ENV=development
PORT=3001
API_PREFIX=/api/v1

# ========================
# Database Configuration
# ========================
# Use any database provider to create the databases and use its url here.
# You can also create local postgres database using the instructions given in setup/DOCKER_DATABASE_SETUP.md file
DATABASE_URL=your-db-url

# ========================
# JWT Configuration
# ========================
# Secrets for signing JWT tokens. Generate a secure random string for each using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET=your_access_secret_here
JWT_REFRESH_SECRET=your_refresh_secret_here
JWT_ACCESS_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=7d

# IMPORTANT: JWT_ACCESS_SECRET must match the one in .env for api-gateway

# ========================
# SES Configuration
# ========================
# Email credentials from any email provider (e.g., Brevo, SendGrid)
# Provide SMTP host, port, user, pass, and the "from" email address
SES_SMTP_HOST=smtp-host
SES_SMTP_PORT=smtp-port
SES_SMTP_USER=smtp-user
SES_SMTP_PASS=smtp-pass
SES_SMTP_FROM=smtp-from

# ========================
# SMS Configuration - FAST2SMS
# ========================
# Default values are usually fine for local development.
FAST2SMS_API_URL=value
SMS_TEST_NUMBERS=value

# ========================
# Security Configuration
# ========================
# General security settings. Usually defaults are fine.
BCRYPT_SALT_ROUNDS=12
OTP_EXPIRES_IN=10
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=15

# ========================
# CORS Configuration
# ========================
# Add frontend URLs that will call this backend service.
CORS_ORIGIN=http://localhost:8080,http://localhost:8081

# ========================
# Service Configuration
# ========================
# URLs and timeouts for other internal services. Usually defaults are fine for local dev.
SCHOOL_SERVICE_URL=http://localhost:3002
SCHOOL_SERVICE_TIMEOUT=30000
