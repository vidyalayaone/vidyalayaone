# ========================
# Server Configuration
# ========================
# General server settings. Usually, you can keep NODE_ENV=development and API_PREFIX as is.
NODE_ENV=development
PORT=3002
API_PREFIX=/api/v1

# ========================
# Database Configuration
# ========================
# Use any database provider to create the database and use its URL here.
# You can also create a local Postgres database using the instructions in setup/DOCKER_DATABASE_SETUP.md
DATABASE_URL=your-db-url

# ========================
# CORS Configuration
# ========================
# Add frontend URLs that will call this backend service.
CORS_ORIGIN=http://localhost:8080,http://localhost:8081

# ========================
# Auth Service Configuration
# ========================
# URL and timeout for the Auth Service. Usually defaults are fine for local development.
# Make sure the URL matches the actual Auth Service port in your setup.
AUTH_SERVICE_URL=http://localhost:3001
AUTH_SERVICE_TIMEOUT=30000
